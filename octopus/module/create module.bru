meta {
  name: create module
  type: http
  seq: 1
}

post {
  url: {{URL_DOMAIN}}/module
  body: json
  auth: inherit
}

headers {
  Authorization: {{TOKEN}}
}

body:json {
  {
    "label": "Como criar um database otimizado"
  }
}
